# FRESHNESS — hub-coverage-audit

体检日期：2026-09-01 · 覆盖式文件，只保留最新一次。
生成者：`hub-coverage-audit`（本地只读体检）。

---

## 1. 登记缺口

**无。37 个内容站六处登记全齐。**

- 磁盘 43 仓：内容站 37，非内容站 6（hub / cissy0802.github.io / cissychen-homepage / video-pipeline / AI-Agent / ai-code-review）。
- 四处从真源解析的登记点（`generate_hub.py` 卡片、`build-search.yml` 克隆循环、`sitemap.xml`、`verify-routine-caps` 守卫）全部对齐，无缺项。
- caps 守卫的书面豁免仍是 7 个：cross-ref-sync、deep-reading、deep-research、mental-models、monthly-frontier、synthesis、weekly-digest。
- **Pages 状态：37 个内容站逐个查 `gh api /repos/cissy0802/<r>/pages`，全部 `built`，无掉线。**
- 登记表里有、磁盘上没有的名字：无。

---

## 2. 可增补的合成文

### syn-3《合作：三本账，和一张掀翻的桌子》—— 建议增补，两个源页、三处落点

已用 15 篇，基准日 2026-07-31（`augmented` 尚为 null）。

#### 源页 A · `/evolutionary-biology/evolution-of-cooperation-day8.html`（新增于 2026-09-01）

**→ 补进 §2「第一本账：记在基因名下」末尾**

syn-3 现在写「吸血蝙蝠的反哺，正是教科书级的例子」——一句带过，无证据无引文。day8 给出实测：Wilkinson 1984 报告非亲缘个体在圈养下照样互喂；Carter & Wilkinson 2013 用两年时间对 20 只蝙蝠逐只禁食、制造出 48 个喂食场面，发现**「这只过去喂过我多少」比亲缘更能预测谁会喂它**，而且多数时候是吃饱的那只主动凑过去。

这条不只是补个引文。**它在 syn-3 自己开篇那个蝙蝠巢房间里，把第一本账（r）和第二本账（w）放在同一组数据上比出了高下**——§5 说「r、w、可信承诺是同一个变量在三种货币里的三副面孔」，而这里恰好有一个系统能把两者分开测、并且分出了强弱。是对 §5 那条承重论断最便宜也最硬的一个约束。

**→ 补进 §3「第二本账：记在重复的名下」**

两条，都短：

1. **以牙还牙一场单挑都没赢过。** syn-3 列了 TFT 四原则（善良／可报复／宽容／透明），却漏掉这一句——它从不先动手，对上任何对手最多打平，拿的是所有对局加起来的总收成。这直接改写「赢」的定义：不是压过对手，是在一堆关系里总产出高。放在「政治资本」那段之前，正好接住后面组织版的用法。
2. **间接互惠补一个非人类证据。** syn-3 这块全是人和平台（q>c/b、Uber、Airbnb、马格里布商人）。day8 给了清洁鱼：Bshary & Grutter 2006 发现旁边有别的鱼看着时，清洁鱼咬客户黏液更少、活干得更老实，而排队的鱼确实在挑刚才好好干活的那条。**要连 day8 那条克制一起带过来**——非人动物的这类证据弱得多，清洁鱼一例也有更简单的解释在场（旁边有鱼看着时被咬的风险本来就不一样）。

#### 源页 B · `/complexity-science/evolutionary-games-cooperation-topic29.html`（新增于 2026-08-15）

**→ 补进 §4「第三本账：记在可信承诺的名下」**

这本账目前从头到尾是霍布斯—利维坦—核威慑—制度性信任，全部押在「造一个可信的外部执法者」上。**Ostrom 完全缺席，而她的发现正是这本账的反例**：瑞士山村高山牧场、西班牙灌溉渠、菲律宾水利组织、日本入会地，既没国有化也没私有化，稳定运转几百年（1990 年八条设计原则，2009 年诺奖）。她那八条里有三条恰好把三本账的三种货币各钉一颗钉子——边界要清楚（不划边界你根本没在玩重复博弈，因为对手是谁都不确定）、监督由使用者自己做（背叛得看得见）、制裁要分级（重罚的问题不是不狠，是狠到没人愿意执行）。

补进去之后，第三本账从「必须有利维坦」修正成「必须有可信的代价来源，利维坦只是其中一种实现」——这正是 topic29 对霍布斯那段越界联想的落点。顺带可带上 topic29 纠的那个误读：Hardin 描述的是无人管的开放资源，而「公地」历史上一向有边界、有规矩、有成员名册。

**→ 补进 §5「共同骨架」末段（合作会怎么死）**

syn-3 现在把崩溃条件写成一串定性断言：群体过大、未来阴影缩短、信息噪声、惩罚缺位。topic29 把其中两条变成可算的：

- **w ≥ (T−R)/(T−P) = 0.5**——合作收益随 w 复利式地涨，占便宜那 2 分是一次性的、不复利；w 一过 0.5，一次性好处就被复利追上。「同一个人、同一套收益，w 从 0.4 挪到 0.6，理性的做法就从背叛翻成合作」，不等式里没有任何关于人品的量。
- **b/c > k**（Ohtsuki、Hauert、Lieberman & Nowak 2006）——k 是网络里每人平均邻居数，**邻居越多合作越难长出来**。b/c 由事情本身决定、改不了；k 几乎总能改（把 200 人的大群拆成二十个 10 人小组，门槛掉一个数量级）。这条正是把 syn-3 那句「群体过大」从断言变成杠杆。

这一步是把 §5 从 syn-3 §8 自己划的第二档（说得清机制、报不出数）提到第一档（报得出货币和数值，可以提货）——按这篇文章自己的标准，是它最该做的一次升级。

**→ 补进 §7「断裂点：一张可以逐条勾的判据」**

加第六问，而且它应当排在现有五问**之前**：

> **这张收益矩阵是测出来的，还是我编出来的？**

现有判据一问「它在用哪种货币算账」，默认那张矩阵存在。topic29 §06 指出真实冲突里那四个数往往测不出来，双方的排序可能根本不同（你以为在打囚徒困境，对方在打鹰鸽博弈，或者压根不在乎输赢、只在乎不能显得输了）——这种时候矩阵不是分析工具，是一件事后叙述的外衣。操作化检验：**至少要能说出「如果我把这个数改成另一个，结论会怎么变」；答不上来，就是在用博弈论的词汇复述一个本来就有的判断。**

同处可补 **反社会惩罚**（Herrmann、Thöni & Fehr 2008，十六地实验）：有些地方被扣分的不是投得少的人，是投得多的人，允许惩罚并没有让合作变好。syn-3 §5 只说「惩罚缺位」会杀死合作，没说**惩罚在场也可能反着来**——它还挂着一个前提：大家对什么该被罚看法大体一致。前提不在，多一件武器只是多一种互相伤害的方式。

另可一并带上 day8 那个更基础的洞：**惩罚本身是一件新的公共品**（花自己的钱、好处所有人分），于是「享受别人惩罚的成果、自己从不动手」的人过得更好——二阶搭便车。这是三本账逻辑内部的一个洞，正是 §7 该收的那类东西。

> 写作提示：两个源页都命中 Fehr & Gächter 2002 与 Herrmann 2008，全文引一次即可，别在 §5 和 §7 各写一遍。

---

### syn-2《进化博弈：同一副底牌，三种文明》—— **不建议增补**

脚本报了 `/complexity-science/evolutionary-games-cooperation-topic29.html`（8 命中）。读过全文后判断：**这 8 次命中是「顺带提了一嘴」。** topic29 通篇讲机制与结构参数（w 门槛、b/c > k、Ostrom 设计原则、矩阵是编的），完全不碰 syn-2 的那根轴线——中国＝亲缘利他、西方＝互惠利他的制度化、印度＝超越博弈本身。唯一沾边的是 §05 的 Ostrom 案例（瑞士／西班牙／日本），但那是公共池塘治理，不是三种文明底牌的比较。

**建议写进 syn-2 的 `evaluated_rejected`**，否则下个月这篇会被当成新发现再报一次。

---

## 3. 顺带发现的异常

1. **`added` 日期竞态（audit.py Part B）。** `/evolutionary-biology/evolution-of-cooperation-day8.html` 报成「新增于 None」。原因：Part B 先跑 `git log` 建 `added` 映射、再扫磁盘建 `TXT`，而这个页面的提交（`Add #8`，2026-09-01）恰好落在两步之间，于是磁盘上有、`added` 里没有。事后复跑 `git log` 能正确取到 2026-09-01。
   影响面：日期取不到时脚本回退成 `'9999'`，**结果是 fail-open**——取不到日期的页面一律通过「基准日之后」这道筛，会被报出来。本次没造成误判（这一页确实是今天新增的），但如果哪天有大量未提交的新页面，Part B 会把它们全报一遍。要不要改成 fail-closed、或者让 `git log` 和磁盘扫描取同一个快照，是取舍，留给 BigCat 定。
2. **syn-2 的 `evaluated_rejected` 是空数组**，syn-3 有 2 条。按上面第 2 节的建议，syn-2 应当补上 topic29。
3. 无其它异常：Pages 全 `built`，登记表与磁盘无名字漂移。

---

## 小结

登记缺口 **0** 处；建议增补的合成文 **1** 篇（syn-3，两个源页、三处落点）；建议记入 `evaluated_rejected` 的 **1** 条（syn-2 × topic29）。

本次为只读体检，除本文件外未改动任何稿子或登记表。
